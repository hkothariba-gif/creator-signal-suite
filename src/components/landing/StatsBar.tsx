// StatsBar previously displayed marketing statistics that were not backed by
// a live data source. It has been removed from the landing page. This stub
// keeps any stale imports compiling while rendering nothing.
export function StatsBar() {
  return null;
}
